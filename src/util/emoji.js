const EMOJI_INCOMING_ENVELOPE = String.fromCodePoint(0x1F4E8)
const EMOJI_HORN = String.fromCodePoint(0x1F4E3)
const EMOJI_STAR = String.fromCodePoint(0x2B50)

const API = {
  EMOJI_INCOMING_ENVELOPE,
  EMOJI_HORN,
  EMOJI_STAR
}

module.exports = API
